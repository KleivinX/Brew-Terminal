// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * One model as the UI sees it: catalogue metadata plus what is actually on disk.
 */
export type ModelStatus = { installed: boolean, 
/**
 * Bytes already fetched into a `.part` file, so a half-finished download is visible
 * rather than looking like nothing happened.
 */
partialBytes: number, id: string, name: string, 
/**
 * What it is, in one line, without marketing.
 */
description: string, parameters: string, quantisation: string, sizeBytes: number, 
/**
 * Rough working memory needed to run it, weights plus context. An estimate, and labelled
 * as one in the UI — it depends on context length and what else is running.
 */
approxRamMb: number, licence: string, publisher: string, sourceUrl: string, };
